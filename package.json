{
  "name": "lime",
  "version": "1.0.0",
  "description": "Message Application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf build && tsc --project tsconfig.json",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' index.ts --verbose",
    "server:dev": "nodemon --watch 'src/server/**/*.ts' --exec 'ts-node' src/server/app.ts --verbose"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vinxenc/lime.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/vinxenc/lime/issues"
  },
  "homepage": "https://github.com/vinxenc/lime#readme",
  "dependencies": {
    "parse-server": "4.5.0"
  },
  "devDependencies": {
    "@types/node": "14.14.31",
    "@types/parse": "2.18.3",
    "@types/express": "4.17.11",
    "nodemon": "2.0.7"
  }
}